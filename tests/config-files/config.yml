servers:
    default: local

    local:
        url: http://imbo
        publicKey: user
        privateKey: key
        active: true

    remote:
        url: http://imbo-remote
        publicKey: remoteuser
        privateKey: otherkey
        active: false
